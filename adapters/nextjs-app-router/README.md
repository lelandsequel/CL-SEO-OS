# Next.js App Router Adapter

This adapter shows how SEO OS integrates into an existing Next.js App Router site.

DO NOT redesign the site.
DO NOT modify existing pages.

Only add:
- /services/[slug]
- /locations/[city]
- /guides/[slug]

All content must be data-driven.
